import { RegisterPage } from "@/components/auth/RegisterPage";

export const metadata = {
  title: "Create Account | SOIS",
  description: "Create a new SOIS account",
};

export default function Page() {
  return <RegisterPage />;
}
