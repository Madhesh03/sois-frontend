import { LoginPage } from "@/components/auth/LoginPage";

export const metadata = {
  title: "Sign In | SOIS",
  description: "Sign in to your SOIS account",
};

export default function Page() {
  return <LoginPage />;
}
